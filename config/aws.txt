aws
- 네이버아이디, 옛날 인증서비번
- 26년 12월에 등록한 우리은행 카드 만료됨
- aws 퍼블릭ip주소(업비트에 등록해야 함): 3.36.131.82

업비트
- 2025.09.24 19:19 만료


*


터미널로 aws 접속
ssh -i C:\workspace\coding\python\bot\config\bot.pem ec2-user@3.36.131.82

.py로 백그라운드(nohup)에서 돌아가고 있는지 확인
ps aux | grep bot/crypto/main.py

프로그램 죽이기
pkill -f /bot/crypto/main.py

기존 폴더 삭제하고 git 저장소 가져오기(clone)
rm -rf bot && git clone https://github.com/11e3/bot.git

코드 백그라운드에서 돌리기
nohup python3 bot/crypto/main.py > /dev/null 2>&1 &


*


현재시간
date

git 설치(안됐을 경우)
sudo yum install git -y