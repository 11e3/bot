aws
- 네이버아이디, 옛날 인증서비번
- 26년 12월에 등록한 우리은행 카드 만료됨
- aws 퍼블릭ip주소(업비트에 등록해야 함): 52.78.64.72

업비트
- api 2025.09.24 19:19 만료


*


터미널로 aws 접속
ssh -i C:/workspace/coding/python/bot/config/bot.pem ec2-user@52.78.64.72

bot폴더로 이동 (필수. 무조건 /bot에서 실행시켜야함 - 제작환경 경로가 이 폴더임)
cd bot

.py로 백그라운드(nohup)에서 돌아가고 있는지 확인
ps aux | grep crypto/main.py

프로그램 죽이기
pkill -f crypto/main.py

로컬에서 aws로 바로 복사하기
scp -i C:/workspace/coding/python/bot/config/bot.pem -r C:/workspace/coding/python/bot ec2-user@52.78.64.72:/home/ec2-user/

기존 폴더 삭제하고 git 저장소 가져오기(clone) (gitignore된 파일은 복사 안 됨 - 사용 금지)
rm -rf bot && git clone https://github.com/11e3/bot.git

코드 백그라운드에서 돌리기
nohup python3 crypto/main.py > /dev/null 2>&1 &

코드 그냥 돌리기
python3 crypto/main.py

나가기
exit


*


현재시간
date

git 설치(안됐을 경우)
sudo yum install git -y